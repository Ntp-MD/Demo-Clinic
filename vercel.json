{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install --prefer-offline --no-audit",
  "framework": "vite",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
